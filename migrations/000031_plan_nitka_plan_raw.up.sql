-- ============================================================================
--  000031_plan_nitka_plan_raw — сырой текст «Плана» нитки.
--
--  В файле плана вместо времени бывает текст («не подводить», пусто и т.п.) —
--  такая нитка реальна (поезд с индексом), подводиться не будет, но в таблице
--  отображается; текст показываем в колонке «План». Прод-фикс формата gtport:
--  нитки распознаются по валидному индексу 4-3-4 / с.ф., № п/п не требуется.
-- ============================================================================

ALTER TABLE dpport.plan_nitka ADD COLUMN IF NOT EXISTS plan_raw text NOT NULL DEFAULT '';
