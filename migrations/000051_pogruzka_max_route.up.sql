-- 000051: маршруты MAX формы «Погрузка» (отчёт страницы «Справки и отчёты»).
--
--  Клиентские маршруты (АЭ/ГУТ-2/УТ-1 → чаты) переехали в пер-клиентский сид
--  scripts/clients/gtport.sql — миграции остаются нейтральным ядром. Схемных
--  изменений у миграции не было; файл сохранён no-op'ом ради непрерывной
--  нумерации golang-migrate (боевые базы её уже прошли).

SET search_path TO dpport;
