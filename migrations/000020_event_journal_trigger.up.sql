-- ============================================================================
--  000020_event_journal_trigger — что запустило обновление снимка дислокации.
--
--  Журнал обновлений дислокации: одно событие = одно перестроение снимка. Помимо
--  источника данных (lk/json) фиксируем ТРИГГЕР — что вызвало обновление:
--    manual        — ручное обновление (пользователь запустил обработку ЛК);
--    scheduled     — по расписанию (фоновый воркер / приём АСУ) [задел];
--    actualization — актуализация (пересчёт текущего снимка без новых данных) [задел];
--    plan          — загрузка плана подвода (проставляет PlanMsk → перезапись снимка).
--
--  «Кто» уже в actor (пусто = система). Кол-во вагонов — в detail (count/stamped).
--  Идемпотентно (ADD COLUMN IF NOT EXISTS). Прежние строки → trigger '' (в выборке
--  доопределяется по event_type).
-- ============================================================================

SET search_path TO dpport;

ALTER TABLE dpport.event_journal
    ADD COLUMN IF NOT EXISTS trigger text NOT NULL DEFAULT '';
