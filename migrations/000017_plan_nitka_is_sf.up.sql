-- ============================================================================
--  000017_plan_nitka_is_sf — флаг строки сборного формирования (с.ф.) в сетке плана.
--
--  Парсер теперь эмитит с.ф.-строки (раньше пропускались): у них индекс «с.ф.<СТАНЦИЯ>»,
--  индексно они не матчатся — группы вагонов выберет пользователь (диалог, отдельный
--  поток prepare/confirm). Флаг нужен фронту, чтобы отличать такие строки.
--
--  Аддитивно и идемпотентно (ADD COLUMN IF NOT EXISTS).
-- ============================================================================

SET search_path TO dpport;

ALTER TABLE dpport.plan_nitka
    ADD COLUMN IF NOT EXISTS is_sf boolean NOT NULL DEFAULT false;
