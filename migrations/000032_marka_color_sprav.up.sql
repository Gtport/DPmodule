-- ============================================================================
--  000032_marka_color_sprav — цвет строки и запасное поле в словаре marka.
--
--  color   — цвет отображения вагонов этой атрибуции в UI (hex, как в старом
--            GTport; в оригинале однозначен внутри ключа okpo+станция+группа).
--  sprav_1 — запасное текстовое поле «на будущее» (решение владельца); в
--            оригинале sprav_1 неоднозначен внутри группового ключа, поэтому
--            данные из него НЕ переносятся — колонка стартует пустой.
--
--  Значения color заливаются seed-скриптом (scripts/seed_directories.sql).
-- ============================================================================

ALTER TABLE dpport.marka
    ADD COLUMN IF NOT EXISTS color   text NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS sprav_1 text NOT NULL DEFAULT '';
