# Secrets — injected by CI/CD from Vault, never stored in config.yaml or git.

# PostgreSQL
PG_PASSWORD=iqport

# Keycloak (only for confidential clients)
KEYCLOAK_CLIENT_SECRET=

# External API keys — add as needed
# SOME_API_KEY=
