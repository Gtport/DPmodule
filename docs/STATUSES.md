# Справка по статусам дислокации

Статус вагона — производная величина, которую движок вычисляет на **Stage 1**
(обогащение) для каждой записи снимка. Он отражает, **где вагон и что с ним
происходит** (на отправлении / в пути / простаивает / брошен / прибыл / выгружен).
На статус опираются последующие шаги: прогноз хода, кандидаты прибытия, доноры
перегруза, простановка плана подвода.

- **Где считается:** `internal/service/enrich.go` → `computeStatus()` (§3.13).
- **Когда:** Stage 1, шаг 4 «статусы и производные» (после станций, порта, операций).
- **Тип:** `dislocation.status` (`*int`), распределение по батчу — в `StatusDist`.

> Время везде московское naive (без таймзоны). «Сейчас» — из `clock.Now()`.

---

## Дерево расчёта (порядок важен)

Статус определяется **первым** подходящим условием сверху вниз. Ключевые признаки:

- **`atDest`** — вагон на станции назначения: `station_oper == stan_nazn` (обе непусты).
- **`ПорожПризнак`** (`porozh_priznak == "1"`) — вагон порожний.

```
1. Порожний? ──да──► atDest? ──да──► 12  (порожний в порту / выгружен)
   │                 └──нет─► 6   (порожний в пути → донор перегруза)
   │нет
2. atDest (гружёный на назначении)? ──да──► date_prib заполнен? ──да──► 10 (прибыл)
   │                                        └──нет──────────────► 9  (кандидат в прибывшие)
   │нет
3. На станции отправления (code_station_nach == code_station_oper)?
   │            ──да──► Index == "Б/И"? ──да──► 0 (без индекса)
   │                                     └нет─► 1 (на отправлении)
   │нет
4. Брошен (code_oper == "92")? ──да──► 5
   │нет
5. Долгий простой в пути (простой ≥ порога)? ──да──► 4
   │нет
6. ────────────────────────────────────────────────► 2 (в пути)
```

---

## Таблица статусов

| Код | Значение | Условие (кратко) | В снимке | Прогноз хода |
|:--:|---|---|:--:|:--:|
| **0** | На отправлении, без индекса | на ст. отправления, `Index == "Б/И"` | да | да (+12 ч буфер) |
| **1** | На станции отправления | на ст. отправления, индекс есть | да | да |
| **2** | В пути | значение по умолчанию | да | да |
| **4** | Долгий простой в пути | простой ≥ порога, не брошен, в пути | да | да |
| **5** | Брошен | `code_oper == "92"` | да | да |
| **6** | Порожний в пути | порожний, не на назначении | да | да |
| **8** | Пропал | был в снимке, нет в новой выгрузке | **нет** (кандидаты) | — |
| **9** | Кандидат в прибывшие | гружёный на назначении, `date_prib` пусто | да | нет (на месте) |
| **10** | Прибыл | гружёный на назначении, `date_prib` есть | да | нет |
| **12** | Порожний в порту / выгружен | порожний на назначении | да | нет |

> Коды **3, 7, 11** не используются — в дереве расчёта их нет (пропуски нумерации).

---

## Каждый статус подробно

### 0 — на станции отправления, без индекса
Вагон стоит на своей станции отправления, но поездного индекса ещё нет
(`Index == "Б/И"`). Операционный буфер: прогноз хода строится, но к расчётному
прибытию добавляется **+12 часов** буфера (`forecast.go`, `computeRaschMsk`).

### 1 — на станции отправления
То же место (ст. отправления = ст. операции), но индекс уже присвоен — вагон
готов к отправлению / формируется. Прогноз хода строится.

### 2 — в пути
Значение по умолчанию: ни одно спец-условие не сработало. Вагон движется между
станцией отправления и назначения. Основной «рабочий» статус для расчёта хода.

### 4 — долгий простой в пути
Вагон стоит в пути (станция операции ≠ отправления и ≠ назначения) дольше порога:
`prost_dn ≥ prost_dn_min` (сутки) **или** `prost_ch ≥ prost_ch_min` (часы).
Пороги — из настроек `client_settings.extra.status` (значения GTport: **1 сутки /
12 часов**). Для агрегации таких вагонов заполняется ключ **`id_status4`**
(`index|code_station_oper|time_op`).

### 5 — брошен
`code_oper == "92"` (операция «брошен»). Заполняется ключ агрегации **`id_status5`**
(та же схема `brosKey`).

### 6 — порожний в пути
`ПорожПризнак == "1"` и вагон **не** на станции назначения. Такой вагон при
**переходе** на статус 6 становится **донором перегруза** (§3.16): его груз/назначение
могут быть перенесены приёмнику без груза (§3.17, `applyStatus6Transition` /
`applyStatus6Donorship`, `stage2.go`). Переход **из статусов ≥ 10** (прибыл/выгружен)
донором **не считается**: груз доехал и выгружен у нас, порожний отъезд штатный.

### 8 — пропал (missing)
**Особый статус — не из `computeStatus`.** Назначается в `reconcileCandidates`
(`stage2.go`) вагонам, которые были в прошлом снимке, но отсутствуют в новой выгрузке.
Такие записи пишутся в таблицу кандидатов (`status9`) со статусом 8 и **в снимок
`dislocation` не попадают** (комментарий в коде: «Статус-8 в снимок НЕ идёт»). Если
вагон возвращается живым (снова 9) — защищённый 8 снимается.

**Не считаются пропавшими** вагоны, выбывшие штатно: статус актуальной записи
**6** (порожний в пути — донор уехал), **10** (прибыл) или **12** (выгружен) —
рейс завершён, порожняк уезжает из поля зрения без записи-8. В `status9` со
статусом 8 попадают только потери **незавершённого** рейса (0, 1, 2, 4, 5, 9).

**Автоочистка:** записи-8 старше `missing8_ttl_days` суток (порог в
`client_settings.extra.status`, у нас 7; 0 → выключена) удаляются на каждом
пересборе снимка (S2-1c, после `reconcileCandidates`) — по таким вагонам
возврата уже не будет. Живые кандидаты (9) не затрагиваются независимо от
возраста. Счётчик — `status8_purged` в статистике обработки.

**Экран:** «Пропавшие вагоны» (`/missing`, роли диспетчер/админ) — последняя
известная позиция каждого пропавшего, `GET /dislocation/missing`.

### 9 — кандидат в прибывшие
Гружёный вагон уже на станции назначения (`atDest`), но факт прибытия ещё не
подтверждён (`date_prib` пусто). «Почти прибыл». Живые кандидаты статуса 9 при первом
появлении пишутся и в таблицу кандидатов (S2-1). Прогноз хода для 9 не строится
(вагон уже на месте).

**Ручное подтверждение (07.2026, отличие от gtport):** оператор видит кандидатов
в «Истории прибывших» (жёлтая секция + бейдж карточки) и либо **подтверждает**
прибытие с фактическим временем — в снимок пишутся статус 10 + `date_prib` +
`date_kon` (дальше факт держит sticky-10 при пересборках, запись-кандидат
снимается reconcile'ом), веха уходит в `vagon_history` сразу; либо **отклоняет**
(«скрыть до новых данных») — пометка `dismissed_at` на записи-кандидате, вагон
остаётся 9 и при появлении `date_prib` из АСУ станет 10 автоматически.
Ручки: `GET /dislocation/arrivals/candidates`, `POST …/confirm`, `POST …/dismiss`.

**Отмена прибытия (07.2026, переосмысление gtport):** обратная операция —
в снимке 10 → 9 (сброс `date_prib`, `date_kon` из `time_op`), вагон снова
кандидат, веха прибытия в `vagon_history` очищается (`POST …/cancel`). Только
для статуса 10 текущего снимка; выгруженным (12) — запрет (выгрузка доказывает
прибытие). С потоком не спорим: если дату давала АСУ, ближайший пул вернёт 10.

### 10 — прибыл
Гружёный на станции назначения с заполненным `date_prib`. Факт прибытия. Для истории
(`vagon_history`) это веха прибытия (`date_prib`). При загрузке плана подвода
плановые данные прибывших **не очищаются** (`planmatch/apply.go` бережёт статусы ≥ 10).
`date_kon` для 10 берётся из `date_op_jd` (см. ниже).

**Заморозки на 10 нет** (отход от эталона gtport, решение владельца 07.2026):
запись прибывшего продолжает обновляться свежими данными РЖД, и после выгрузки
вагон честно переходит **10 → 12** — веха выгрузки уходит в `vagon_history`.
От «мигания» `date_prib` (свежая выгрузка временно потеряла дату прибытия)
защищает **sticky-10** (см. «Удержание статуса»). Квирк gtport «у прибывшего
показывается плановая нитка вместо индекса» (`Index = IndexPp`) упразднён.

### 12 — порожний в порту / выгружен
`ПорожПризнак == "1"` и вагон на станции назначения. Трактуется как выгрузка в порту.
Для истории — веха выгрузки (`date_vigr`, `date_vigr_d`, `place_vigr`). Обычный
путь — переход **10 → 12** после выгрузки (заморозки на 10 нет); строка рейса в
`vagon_history` в итоге содержит и прибытие, и выгрузку. План-поля у 12, как и у 10,
не очищаются и не проставляются заново.

---

## Производные поля, зависящие от статуса

Считаются там же, в Stage 1, шаг 4 (`enrich.go`):

- **`date_kon`** (`computeDateKon`): для статуса **10** — `date_op_jd` (ЖД-время
  прибытия), для всех прочих — `time_op` (время последней операции).
- **`id_status4` / `id_status5`** (`brosKey`): ключ агрегации брошенных (5) и
  простаивающих (4) — `index | code_station_oper | time_op`. Пуст, если нет индекса /
  станции / времени операции.
- **`delay`** (`computeDelay`): просрочка доставки в сутках, если норматив
  `date_dostav` в прошлом относительно «сегодня» (МСК); иначе `nil`.
- **`id_disl`** (`computeIdDisl`): ключ поезда `index | code_station_oper | oper_s |
  date_op(ДД.ММ.ГГГГ)` из непустых компонентов — используется для группировки/матча.

---

## Удержание статуса (sticky 4/5/10)

При переносе из прошлого снимка (`carryover.go`, `enrichFromActual`) статусы **4**
(простой) и **5** (брошен) **удерживаются**, пока вагон стоит на **той же** станции
операции (`code_station_oper` не изменился). Это гасит «мигание» статуса между
выгрузками, когда свежие данные РЖД временно дают другой код. Как только станция
операции меняется — статус пересчитывается заново.

**Sticky-10:** если вагон был «прибыл» (10), стоит на той же станции операции, а
новый расчёт дал **9** (то есть свежая выгрузка потеряла только `date_prib`) —
удерживаются статус 10, `date_prib` из актуальной и `date_kon` (= `date_op_jd`).
Любой другой новый статус (например, 12 после выгрузки) проходит без удержания.

---

## Влияние статуса на прогноз хода

`forecast.go` (Stage 3, S2-5) считает время до порта (`ToGo → RaschMsk / RaschJd`)
только для вагонов **в пути**:

- **считается** для статусов `< 9` (0, 1, 2, 4, 5, 6); для статуса **0**
  (операционный буфер) к `RaschMsk` добавляется +12 часов;
- **пропускается** для статусов `≥ 9` (9, 10, 12 — вагон уже на месте).

---

## Пороги и настройки

| Параметр | Где | Значение GTport | На что влияет |
|---|---|---|---|
| `prost_dn_min` | `client_settings.extra.status` | 1 (сутки) | порог простоя → статус 4 |
| `prost_ch_min` | `client_settings.extra.status` | 12 (часов) | порог простоя → статус 4 |
| `missing8_ttl_days` | `client_settings.extra.status` | 7 (у нас; в GTport не было) | автоочистка записей-8 в `status9`; 0 → выключена |

---

## Ссылки на код

- Дерево статусов: `internal/service/enrich.go` → `computeStatus`.
- Производные (`date_kon`/`delay`/`id_disl`/`id_status4/5`): `internal/service/enrich.go`.
- Удержание 4/5/10: `internal/service/carryover.go` → `enrichFromActual`.
- Пропавшие (8), кандидаты (9): `internal/service/stage2.go` → `reconcileCandidates`.
- Доноры/донорство перегруза (6): `internal/service/stage2.go` → `applyStatus6Transition` / `applyStatus6Donorship`.
- Прогноз хода (статус `< 9`): `internal/service/forecast.go`.
- План бережёт статусы ≥ 10: `internal/service/planmatch/apply.go` (очистка) и
  `planmatch.go` → `collectVagons` (простановка).
