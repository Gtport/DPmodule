# DPmodule — GTport Universal (активный и единственный проект)

> Точка входа для Claude Code. **Источник правды по процессу — `PROJECT_INSTRUCTIONS.md`**
> в этом каталоге; здесь — только выжимка и навигация. Инфраструктура VPS —
> в `../CLAUDE.md`. При конфликте `.md`: `PROJECT_INSTRUCTIONS.md` → `TARGET.md` → остальные.

## Задача (одна, надолго)
Переписать **GTport** (рабочий инструмент диспетчера логистического центра,
учёт дислокации вагонов) из жёстко заточенного под 3 порта (`АЭ`/`УТ-1`/`ГУТ-2`)
в **универсальный** прототип, настраиваемый под любое предприятие: порт-специфичный
хардкод → в БД, парсеры/структуры — расширяемые.

**Способ — greenfield (зафиксировано):** пишем заново на шаблоне `TMPL_backend`
и переносим бизнес-логику из `gtlogic`. Не доработка старого кода по месту.
- `gtlogic` (= старый GTport) — источник **бизнес-логики и правил** (референс, не трогать).
- `TMPL_backend` — источник **архитектуры** (Ports & Adapters, DI в `main.go`).
- `DPmodule` — то, что пишем (seeded из `TMPL_backend`).

## Среда Claude Code (отличие от чат-воркфлоу в PROJECT_INSTRUCTIONS)
`PROJECT_INSTRUCTIONS.md` писался под воркфлоу «код приходит в чат». Здесь, в
Claude Code на VPS, **репозиторий на диске — читай код напрямую** (Read/Grep),
а не жди вставки. База знаний (`.md`) остаётся стабильным контекстом и обновляется
по ходу. Один заход — одна логическая задача (подсистема / 1–2 связанных файла).

## Два клона (WSL — разработка, VPS — бой) — воркфлоу
Проект ведёт **один человек — владелец**. Клоны разнесены по машинам, синхронизация
только через GitHub (`git@github.com:Gtport/DPmodule.git`):
```
<WSL>/projects/DP/DPmodule          ← разработка: код, тесты, экраны
/home/alex/projects/DPmodule        ← VPS, боевой стенд: только pull + пересборка
```
**Разработка переехала в WSL** (2026-07-25). Разделение ролей:
- **WSL** — пишем код, гоняем `go test`, смотрим экраны на своей копии базы и
  своём Keycloak. Внешние интеграции (АСУ, MAX, памятки, крон «Брошенные») там
  **выключены** — второй инстанс не должен слать формы в живые чаты и дублировать
  забор данных. Конфиг машины разработчика — `config.local.yaml` (вне git,
  шаблон `config.local.example.yaml`), запуск `-config config.local.yaml`.
- **VPS** — обычно только `git pull`, пересборка и рестарт (скил `/deploy`). Боевая
  база, с ней прямо сейчас работает диспетчер, и проверить правку там не на чем:
  своей копии базы нет. Поэтому правим здесь лишь по необходимости — и с тем же
  ритуалом коммита и пуша, что в WSL.
- Копия боевой базы в WSL обновляется `scripts/dev_db_refresh.sh` (дамп снимается
  на VPS по ssh, льётся в локальную базу; боевая только читается). Для разведки
  боевых данных — SSH-туннель ролью `dpport_ro` (только SELECT,
  `scripts/dev_pg_readonly.sql`). Пошагово — **`docs/DEV_WSL.md`**.

Правила (обязательны для владельца И для ассистента):
- **Коммитим прямо в `main`** (соло, решение владельца 2026-07-27). Ветка и PR —
  не обязательный ритуал, а инструмент: заводим ветку, когда работа идёт несколько
  заходов или трогает миграции/конвейер дислокации, то есть когда `main` нужно
  сохранить раскатываемым под возможный хотфикс. Порядок — скил `/commit`.
- **Гейт — `git push`, а не коммит.** `origin/main` — источник деплоя (`/deploy`
  на VPS делает `git pull`). Инвариант: **всё, что ушло в `origin/main`, пригодно
  к раскатке.** Локальные коммиты — свободно; пуш — только когда
  `go build ./...` и `go test ./...` зелёные.
- **Работаем на одной машине за раз** (правки поочерёдные, не параллельные). Машина,
  с которой уходим, **не держит ничего незапушенного** — см. «Начало и конец сессии».
- **Писать код лучше в WSL:** там копия базы, выключенные интеграции и `go test`.
  На VPS правим, когда иначе нельзя (диспетчер стоит, чинить надо сейчас) — и сразу
  после этого коммитим и пушим. Незакоммиченная правка на VPS не пропадает, но
  ведёт себя подло: либо блокирует следующий `pull` («local changes would be
  overwritten»), либо расходится с git молча — `go build` там собирает **из рабочего
  дерева**, и правка уезжает в боевой бинарь невидимо для git.
- **`_reference/seed/` вне git — живёт в каждой папке отдельно.** При обновлении
  справочников синхронизировать вручную (`cp -r`); git их НЕ переносит. Копирование
  мимо git — только для того, что вне git (seed, `config.yaml`, `.env`).
- **Второй `ng serve` на той же машине займёт порт 4200** → конфликт. Dev-сервер
  держим один: на VPS это systemd-юнит `dpmodule-frontend`, в WSL — `npm start`.

## Начало и конец сессии (работа поочерёдно на двух машинах)
Владелец работает то в WSL, то на VPS — **никогда одновременно**. Значит конфликтов
правок по построению нет, а единственный реальный риск — **уйти с машины, не запушив**:
вторая сторона увидит устаревшую картину и примет её за полную.

**Инвариант: машина, с которой ушли, не держит ничего незапушенного.** Тогда git —
полная правда о проекте, и обе стороны это знают.

Начиная сессию (ассистент делает это сам, не дожидаясь просьбы):
```bash
git pull --ff-only
git status -sb          # ждём чисто, без ahead/behind
git log --oneline -5    # чем занимались в прошлый раз и на какой машине
```
Расхождение (`ahead`, грязное дерево, отказ `--ff-only`) — **сказать владельцу сразу**,
до начала работы: скорее всего, на прошлой машине что-то осталось незапушенным.

Заканчивая работу: проверки → коммит → `push` → `git status -sb` чист (порядок — `/commit`).
Работа не доделана — **всё равно коммит**, в ветку `wip/<что>`, и `push`. `git stash`
не использовать: стеш лежит только в своём клоне, в `git status` не виден и забывается.

⚠️ **Контекст разговора через git не ездит.** Ассистент на другой машине увидит только
репозиторий — ни этой переписки, ни принятых в ней решений. Поэтому всё, о чём
договорились, оседает **в том же заходе** в `.md` (раздел «Текущий статус» ниже,
`STRUCTURE.md`, `docs/`) или в сообщение коммита. Не записано — считай, не было.

⚠️ **Что не про конкретную машину — держим внутри `DPmodule/`.** Папка уровнем выше
(`~/projects/DP/`) **не** git-репозиторий: `DP/CLAUDE.md` и `DP/.claude/` — локальные
заметки этой машины, на вторую они не попадут никак.

## Жёсткие правила (нарушать нельзя без явной просьбы)
- **Поведение GTport — эталон.** Новый код даёт те же результаты на тех же входах.
  **Сначала тест (golden), потом перенос.** Перенос — малыми проверяемыми кусками.
- **Никакого нового хардкода** портов. Новые порт-данные → таблицы + `PortRegistry`,
  не `switch`/`map`. Атрибуты порта — через реестр, не `switch port`.
- **GORM-гибрид (канон):** билдер для CRUD и динамических `UPDATE` (только непустые
  поля); сырой SQL дословно через `gorm.Raw().Scan()`/`gorm.Exec()` для аналитики
  (CTE, `DISTINCT ON`, оконные), DDL, атомарной подмены таблиц. ORM-модель отделена
  от доменной. Снапшот-таблицам (`disl_actual`/`disl_new`) — без `gorm.Model`/хуков.
  Схему ведёт `golang-migrate`, **не** `AutoMigrate`.
- **Время — Московское naive, без часовых поясов** (жёсткий инвариант, `TARGET.md`
  §3.11). Всё время (Excel/JSON/БД/API) — Московское без TZ; отдаём как пришло, **не
  корректируем**. Тип `LocalTime` (без `Z`, `2006-01-02T15:04:05`, `timestamp without
  time zone`). «Сейчас» — только по Москве, из **единого** `clock.Now()` (единственное
  место с `LoadLocation("Europe/Moscow")`); штампы `CreatedAt/UpdatedAt` — через него.
  **Запрещено** в коде: `LoadLocation`/`FixedZone`/`.In`/`.UTC()`/сдвиги `Add(±7h/±10h)`.
  Правило «час ≥ 18 → +1 сутки» (`DateNach`/ID) — бизнес-правило, **не** TZ, остаётся.
- **Silo:** один клиент = один экземпляр = одна БД. `enterprise_id` НЕ вводим.
  RAM-движок дислокации (`actualMap` + индексы + обогащение) переносится **как есть**.
- **Падать громко** на целостности данных. Миграции — идемпотентные, только
  добавляющие. Не переписывать модуль целиком без просьбы — минимальное изменение.
- ⚠️ Не путать **«исторические снимки/бэкапы `disl_actual`»** (упраздняем) с
  **бизнес-историей** `vagon_history`/`vagon_operation` (сохраняем).

## База знаний (читать по необходимости — Claude Code их НЕ грузит сам)
- `STRUCTURE.md` — **карта проекта для человека**: текущая структура backend и frontend,
  файл за файлом, с пояснением «за что отвечает». ⚠️ **Поддерживать в актуальном
  состоянии:** при добавлении/удалении/переименовании файла или смене его назначения —
  обновлять `STRUCTURE.md` в том же изменении (той же ветке/PR). Это справочник владельца,
  не давать ему устареть.
- `PROJECT_INSTRUCTIONS.md` — **процесс и все правила целиком** (главный документ).
- `TARGET.md` — целевая универсальная модель (схемы таблиц, реестр, миграция).
  Сверяться **перед** предложениями по архитектуре данных.
- `HARDCODE_INVENTORY.md` — каталог порт-хардкода и чем заменяется. Сверяться при
  работе с порт-специфичными местами.
- `ARCHITECTURE.md` — карта GTport «как есть» (бизнес-логика, НЕ образец нового кода).
- `OPTIMIZATION.md` — план улучшений; многое уже в `TMPL_backend`. Актуальны
  бизнес-правила для golden-тестов и список «что не трогать».
- `_reference/GORM_MIGRATION_GUIDE.md` — приёмы GORM и swap «вариант B» (заливка в
  `disl_new` → `rename` в `disl_actual`). Референс приёмов, не план действий.
- `_reference/*.go`, `*.sql` — исходники из старого GTport как образцы логики.

## Команды
```bash
cd /home/alex/projects/DPmodule
make            # цели сборки/тестов смотреть в Makefile
go test ./...   # тесты (golden-тесты — основная страховка переноса)
go build ./...
```

## Фронтенд (живые экраны — см. «Текущий статус»)
Форк шаблона `../TMPL_frontend` (**Angular 21** + ng-zorro + Keycloak) лежит в
`frontend/`. Окружения `src/environments/*`: `moduleId=dpport`,
`keycloak.clientId=iqport-dpport`, `apiBaseUrl=/api`. Стили — только через
`src/styles/tokens.css`. Dev-сервер: systemd user-юнит `dpmodule-frontend`
(`ng serve` на `127.0.0.1:4200`, `--allowed-hosts`, прокси `/api`→`:8080`);
наружу — nginx `95850.koara.live`→`:4200` с WebSocket-апгрейдом.
`systemctl --user restart|status dpmodule-frontend`.

## Стиль
- Русский; объяснения по делу для логиста-неразработчика.
- Перед заменой хардкода предлагай «реестр/таблица + golden-тест».
- Перед коммитом показывай diff и спрашивай подтверждение.
- **Модалки — перемещаемые** (решение владельца): заголовок каждой nz-modal —
  шаблон с `cdkDrag cdkDragRootElement=".ant-modal-content" cdkDragHandle`
  (класс `.ttl`: cursor move); образец — `features/home/arrivals-history.component.ts`.

## Текущий статус
Конфиги и секреты (решение владельца 31.07.2026, переход на Vault + Jenkins):
секрет объявляется **в самом конфиге** шаблоном `${vault:<движок>/<путь>:<ключ>}`,
значение подставляет CI/CD перед стартом — **своего резолвинга Vault в приложении
нет**, поле читается как обычное значение. Покрыты `postgres.password`,
`keycloak.client_secret`, `keycloak.service_account.client_secret`, `max.bot_token`
— всего четыре секрета, больше в хранилище класть нечего. Приоритет: **файл конфига
→ env**; переменные окружения (`PG_PASSWORD`, `KEYCLOAK_CLIENT_SECRET`,
`KEYCLOAK_SA_CLIENT_SECRET`, `MAX_BOT_TOKEN`) остаются **запасным путём** для стендов
без подстановки — боевой VPS (systemd `EnvironmentFile=`) и машина разработчика
продолжают работать без правки юнита. Адаптеры просят секрет по ИМЕНИ ключа через
`port.SecretSource`; сводит два мира `secret.LayeredSource` (конфиг поверх env).
⚠️ **`asu.token` в конфиге НЕТ** (решение владельца 31.07.2026): к провайдеру ходим
через Keycloak, а `X-API-Key` остался лишь запасным режимом для отката и читает ключ
ИЗ ОКРУЖЕНИЯ по имени, которое объявил источник (`data_source.auth_secret_key` для
АСУ, `reference.auth_secret_key` для памяток). Плейсхолдером это и не закрыть:
источников может быть несколько, и заводятся они данными.
Пути Vault: БД — `${vault:kv/databases/postgres/dev:gtport_app}` (боевая конвенция —
один секрет `databases/postgres/dev`, ключ = имя роли БД); остальные в
`config.yaml` пока по образцу шаблона, **согласовать с DevOps**. ⚠️ Конфиг стенда
называется **`config.yaml` и лежит В GIT** (решение владельца 03.08.2026, принято
из ветки тимлида 05.08): его забирает CI/CD под этим именем, прежнее правило
`.gitignore` снято — секретов в файле нет, только плейсхолдеры Vault. Конфиг
боевого VPS переехал в **`config.vps.yaml`** (содержимое не менялось); ⚠️ при
первом деплое этого изменения systemd-юнит `dpmodule-backend` на VPS надо
переключить на `-config config.vps.yaml`, иначе бой прочитает стендовый
`config.yaml` и уйдёт на чужую базу. Файл машины разработчика — `config.local.yaml`
(по-прежнему вне git).

Авторизация у провайдера (решение владельца 31.07.2026): **X-API-Key упраздняется,
ходим через Keycloak** — сервис-аккаунт, поток client_credentials (человека в цикле
нет, поэтому работает и в кронах), токен в `Authorization: Bearer`. Настройки —
`keycloak.service_account` (`token_url`/`client_id`/`client_secret` шаблоном Vault,
env `KEYCLOAK_SA_CLIENT_SECRET` запасным путём); движок — `adapter/oauth`
(кэш токена до истечения с запасом 30 с). Охват — **всё, что ходит к этому
провайдеру**: дислокация АСУ, запрос 601, памятки ГУ-45 (адрес и ключ у них общие).
X-API-Key **оставлен запасным режимом**: `auth_mode` в `data_source.config` для АСУ
и `reference.auth_mode` в конфиге для памяток (`keycloak` | `apikey`). ⚠️ Пустой
`auth_mode` = keycloak, **только если сервис-аккаунт настроен**, иначе apikey — стенд
без заведённого Keycloak (сегодня это бой) не встаёт после обновления кода, а
переключается заполнением блока. Явный `apikey` сильнее — им откатывают один
источник, не трогая прочие.

Конвейер дислокации Stage 1–4 перенесён и в бою: приём ЛК (двухшаговый) и
автозагрузка АСУ-АСУ (крон + ручная), гарды свежести/потерь/«данные не
обновились», единый журнал событий (`disl_update`/`disl_rejected` с кодом гарда/
`plan_upload`/`dict_reload`; кто/чем/когда).

Робот ЛК РЖД — **фоновый запуск** (решение владельца 01.08.2026, после разбора
жалобы «файлы пришли, а в модалке их не видно»). Причина была не во фронте:
забор двух кабинетов занял 53,7 с при `http.write_timeout: 30s`, Go бросил
запись ответа, nginx отдал браузеру 502 («upstream prematurely closed
connection»), обработчик при этом доработал и сложил оба файла — окно молча
осталось со старым списком. Лечить таймаутами не стали: **скоро CI/CD-стенд,
где модули живут в контейнерах за чужим ingress'ом**, и правило «работает без
перестройки прокси» важнее. Поэтому `POST .../robot/run` отвечает 202 сразу,
работа идёт в горутине с контекстом, отвязанным от запроса (claims переносим
руками, иначе журнал теряет актора; потолок — `lk_robot.run_timeout`, дефолт
30m), а модалка опрашивает `GET .../robot/state`. Долгих HTTP-запросов у нас
больше нет — таймауты прокси приложению безразличны. Робот сам делает и шаг 2:
после забора проверяет приём и обновляет дислокацию, не готово — говорит почему.
Прогресс живёт на сервере, поэтому окно можно закрыть и открыть заново, а
повторный запуск на ходу отклоняется (409). Кнопки на главной: **АСУ · ЛК ·
АВТО ЛК** в один ряд карточки «Статус системы» (сокращены, чтобы влезли;
Alt+A/L/R), автозабор — отдельной перемещаемой модалкой, ручная загрузка
осталась своей.

АВТО ЛК — **без Excel** (решение владельца 03.08.2026). Робот больше не качает
xlsx и ничего не кладёт в приём: дислокацию читает прямо из ответа кабинета на
опрос готовности (`GET reports/{id}?minimal=true`) — тем же, чем ЛК рисует
таблицу на экране. Разведка боем: в ответе **136 полей АСОУП** (67 колонок xlsx
были их ОТОБРАЖЕНИЕМ — склейки «наименование (код)»), значения в формате фида
АСУ, метка формирования среза — `data.created_at` (сверена с меткой из шапки
xlsx того же забора: совпала точно). Поэтому парсер новый не писался: переходник
`ParseRows` (`parser/json_rows.go`) перекладывает строку в ту же `jsonVagon` и
идёт через общий `convert`, а поля берутся по тегам рефлексией. Сверка xlsx vs
JSON на одном срезе (4853 вагона обоих кабинетов): вагоны 1:1, 28 из 32 полей
1:1; расхождения — в пользу JSON (секунды в `DATE_OP`, которые Excel срезает;
станция отправления у 15 вагонов НМТП, где Excel отдавал пустую ячейку и запись
получала временный ID `temp_*`, ломая carry-over) либо безразличны конвейеру
(`0` против `nil` в простое/остатке расстояния — принимающая сторона трактует
одинаково, и это ровно поведение боевого АСУ-пути). ⚠️ Ведущий ноль ОКПО:
кабинет возвращает `01126022` (как мы сами шлём в фильтре), а в снимке и `ports`
ОКПО живёт числом — нормализация в `convert` (`trimOkpoZeros`), иначе один
грузополучатель писался бы двумя строками. Конвейер общий: гарды комплекта
(`LKIntake.checkBatch` — полнота/разрыв срезов/устаревание, вынесена из
`Status`, работает и по файлам, и по потокам), гарды свежести, `ProcessRecords`,
журнал (`ProcessBatch`, источник `lk_robot`) — по образцу `asu_ingest`.
**Ручной приём ЛК не тронут**: своя модалка, свой xlsx-парсер, своя папка,
свой контроль. В состоянии робота файлов больше нет — модалка показывает срез и
число вагонов по кабинету. Сырой ответ можно класть на диск (`lk_robot.dump_dir`,
вне git) — для разбора контракта и golden-тестов.

Вагон нельзя терять из-за справочника (решение владельца 03.08.2026, разбор
«добавилось 15 пропавших НМТП»). Было два наложенных дефекта. Первый: Stage 1
ОТБРАСЫВАЛ запись, если пара (ОКПО+станция назначения) не резолвилась в порт —
это отход от gtport, там `enrichFromPorts` просто ничего не заполняет, а запись
остаётся в дислокации (владелец видел эти вагоны в GTport). Второй: обогащение
станций делало ранний выход при станции, которой нет в справочнике, — и тогда
StanNazn оставался пустым, порт не резолвился, вагон исчезал. Теперь станции
обрабатываются НЕЗАВИСИМО (неизвестный код гасит только свои поля и попадает в
`stations_not_found`), а нерезолвнутый порт запись НЕ выбрасывает: остаётся с
пустым терминалом. Отбрасывается только ВЫКЛЮЧЕННЫЙ порт — это осознанное
скрытие. Счётчик `PortUnresolved` остался диагностикой неполноты справочников.
⚠️ Боевой случай: станция ШУШАРЫ, код `033004` (Октябрьская дорога) — её в
`stations` нет, как и всего диапазона 00xxxx–05xxxx (в справочнике 3124 станции,
в основном восточные дороги). На xlsx-пути дефект маскировался: кабинет печатает
код в скобках БЕЗ ведущего нуля («ШУШАРЫ (33004)»), а парсер ждёт ровно шесть
цифр — поле оставалось пустым, и раннего выхода не случалось. JSON отдаёт честное
`033004` — и вагоны провалились. В БД коды хранятся числом, ведущий ноль роли
не играет; дело было именно в отсутствии станции.

Рейс в vagon_history опознаётся по `trip_key`, НЕ по id строки (03.08.2026).
`trip_key` (уникальный индекс) = вагон + дата начала рейса, а `id` = вагон +
СТАНЦИЯ ОТПРАВЛЕНИЯ + дата. У тех же 15 вагонов станция отправления появилась →
id сменился → рейсы показались новыми → `INSERT` упал на
`uq_vagon_history_trip_key` и оборвал ВСЮ пересборку снимка. Мина лежала давно:
её ронял любой вагон со сменившейся станцией отправления. `applyHistory` теперь
ищет рейс через `HistoryRepository.ExistingTrips` (trip_key → id) и обновляет
строку по её настоящему id, каким бы он ни был (в т.ч. `temp_*`). `DO NOTHING`
сознательно НЕ применяли: он бы убрал падение, но прибытие/выгрузка таких
вагонов молча не попадали бы в историю. Тест регрессии —
`TestApplyHistory_TripFoundWhenIDChanged`.

Админ-редактор: ключ-значение вводится руками (03.08.2026). Форма прятала
колонку-ключ всегда, а у `stations` ключ — сам `kod` (осмысленный 6-значный код
станции), поэтому завести станцию было невозможно. Теперь различаем автоключ
(`serial`/`identity` — по-прежнему скрыт, значение даёт база) и ключ-значение
(показывается при СОЗДАНИИ и обязателен). Признак `auto` вычисляется из схемы
(`is_identity`, `DEFAULT nextval(`), а не списком таблиц, — новые справочники с
осмысленным ключом заработают сами. При правке ключ не показывается (сервер его
игнорирует), при копировании строки не переносится (иначе дубль).

План подвода: ⚠️ **лист верстают руками, и раскладка плывёт день ото дня** (по
книгам за июль: «Индекс» то в B, то в C; «Кол. ваг.» то подписан, то приходит как
«Итого», то отсутствует; уровней шапки 2–4; 29.07 по Находке терминалы встали в
САМУ строку подписей, а «N п/п»/«Индекс» не проставили вовсе — план грузился с
нулями). Поэтому парсер (`internal/parser/plan/grid.go`) ищет геометрию **по
содержимому, а не по смещениям от шапки**, а `collect` падает громко, если у всех
ниток 0 вагонов. Проверка правок геометрии — прогон месячной книги целиком
(`TestAllSheets`, см. `STRUCTURE.md`) до и после: 116 листов обеих станций за июль
разбираются, меняться должны только починенные. Единственная оставшаяся опора на
подпись — «План»; решение владельца 29.07.2026: **парсер дальше не усложнять**, а
подписывать столбец в файле руками. Инструкция диспетчеру показывается **в
интерфейсе**: отказ узнанного вида открывает окно с шагами правки файла
(`features/plan/plan-upload-hint.ts`), неузнанный — тост как раньше; развёрнутая
версия для человека — `docs/PLAN_UPLOAD.md` (там же остальные отказы загрузки). Опора по данным на случай возврата к
задаче проверена: колонка с максимумом времён ЧЧ:ММ = «План» на 116/116 листов.
Остальное: парсер «новой формы» (нитки по валидному индексу 4-3-4 или с.ф.,
№ п/п не требуется; «не подводить» — нитка без времени с виртуальной датой),
матч/скоринг, человеко-в-цикле (правки индексов; с.ф.: группы + поезда-кандидаты
сформированные/уехавшие по префиксу индекса, выбор единственного реального поезда
подставляет индекс сборного в нитку), пересчёт по сохранённой сетке без Excel.

Обогащение груза (решение владельца, PR #66/67/81/82): словарь `cargo` (код ЕТСНГ →
группа/имя/метка, Stage 1, каждому вагону) отделён от бизнес-атрибуции `marka`
(СТРОГИЙ ключ окпо+станция+ГРУППА груза, частичный матч упразднён; + цвет вагона
для UI); sms_2 — расчётный (sms_1 + cargo_sms); наследование по единогласному
составу (S2-3d). «Снимок вернее потока»: атрибутированные вагоны несут груз-поля/
sms/цвет carry-over'ом; правки словарей применяются кнопкой «Обновить справочники»
(`dict_reload`: перезагрузка RAM + гибридный пересчёт снимка + Stage 3–4).

Админ: страница (роль admin) с универсальным редактором справочников по
реестру `list_tables` (marka, stations, sf, naznach_station, cargo) — копия строки,
русские подписи из `COMMENT ON COLUMN`; новый справочник = INSERT-строка в реестр.

Дев-стенд в контейнерах (перенос работы тимлида из архива backend-master,
03.08.2026): фронт и бэк — раздельные docker-контейнеры. Бэкенд: порты
**8580/9580** (`Dockerfile`, `deployments/docker-compose.yml`,
`config.yaml`), healthcheck по `/ready` (пингует базу: unhealthy =
«трафик рано», не «процесс мёртв»), лог-файл в volume `app_logs`, в файл всегда
JSON. `config.yaml` — конфиг ИМЕННО стенда: Postgres `176.53.160.9`,
база `kgdm` (общая с соседями, наши таблицы в схеме `dpport`), роль
`gtport_app`; Keycloak `https://uport1.ru/realms/iqport` (issuer проверен по
discovery 03.08 — доменный, НЕ IP), audience `iqport-dpport`,
`strict_roles: true`; исходящие интеграции выключены. Секреты — шаблоны
`${vault:...}`, резолвит CI/CD; запасной путь — env. Сервис-аккаунт
`iqport_dpport_service` в realm'е ещё не заведён — блок пуст, интеграциям он и
не нужен (выключены). Фронт-контейнер: `frontend/Dockerfile` (production env →
uport1.ru), `frontend/nginx.conf` проксирует `/api` на `api-backend:8580`.
Тест `internal/config/examples_test.go` сторожит парсируемость примеров
конфигов.

Фронт-контейнер доведён до раскатываемого состояния (03.08.2026, проверено
сборкой и запуском; сведено с архивом `frontend-master` тимлида). Раскладка
портов модуля на стенде: **бэкенд 8580 / его метрики 9580, фронт 8581 / его
метрики 9581**; наружу через ingress торчит только 8581. На порту метрик —
`/metrics` для Prometheus (собран из переменных `stub_status`) и сам
`/stub_status` на случай экспортера, остальное 404.
⚠️ Фронт **не поднимался вовсе**: `proxy_pass` с литеральным ИМЕНЕМ nginx
резолвит на СТАРТЕ и падает с «host not found in upstream», если бэкенда ещё
нет в DNS, — crash-loop без страницы логина. С IP-адресом этого не видно
(резолвить нечего), но грабля срабатывает ровно в момент перехода на имя
сервиса. Поэтому адрес идёт переменной `${API_BACKEND}` (envsubst шаблона на
старте) с `resolver`/`resolver_timeout 5s`: резолв ленивый, фронт живёт
независимо от бэкенда, `/api` отвечает 502, пока тот не встанет, а сменить
адрес можно без пересборки образа. `$request_uri` обязателен — с переменной
nginx сам путь не подставляет.
⚠️ **На стенде фронт и бэк — РАЗНЫЕ compose-проекты, общей сети нет** (решение
тимлида, как и для похода к rwgate): дефолт `API_BACKEND` — адрес хоста
(`176.53.160.9:8580`). Сервис `frontend` в `deployments/docker-compose.yml`
сводит пару в одну сеть для ЛОКАЛЬНОЙ проверки одной командой, там адрес —
имя сервиса `app`. Появился `frontend/.dockerignore` (без него 450+ МБ
`node_modules` с машины разработчика ложились поверх `npm ci`), образ сборки —
`node:24-alpine` как в dev и на VPS.
⚠️ **`client_max_body_size 16m` на `/api` — не оптимизация, а починка отказа**
(сверка с шаблоном платформы 04.08.2026): умолчание nginx 1 МБ, а реальный
отчёт ЛК весит 1,22 МБ, и загрузка отлетала бы с 413, не дойдя до бэкенда;
локально через `ng serve` лимита нет вовсе, поломка вылезла бы только на
стенде. Проверено обеими сторонами: без директивы 413, с ней файл доходит.
Там же gzip статики (в образе nginx выключен по умолчанию; `gzip_types`
перечислены явно, иначе жмётся только html — ровно это и происходит сегодня
на VPS) и вечный кэш хешированных бандлов при некэшируемом `index.html`. Проверено: SPA и fallback 200, метрики и
`stub_status` отдаются, путь `/api/v1/...` доходит целиком и по IP, и по имени
сервиса, при упавшем бэкенде фронт остаётся healthy и отдаёт 502 за 0,2 с.

Вход переведён на **платформенный флоу** (решение владельца 03.08.2026, после
сверки с шаблоном `iqport-frontend-template`): Authorization Code + PKCE на
hosted-странице Keycloak через `keycloak-js` + `keycloak-angular`
(`provideKeycloak` в `app.config.ts`, `onLoad: login-required`). Своя форма
входа и `grant_type=password` (ROPC) упразднены вместе с маршрутом `/login`,
компонентом страницы и своим `auth.interceptor.ts` — Bearer вешает
`includeBearerTokenInterceptor` по условию «URL начинается на /api», токен он
же тихо обновляет. Почему: пароль проходил через наш код, refresh-токен лежал в
localStorage, SSO между модулями не было, а на клиенте требовался включённый
Direct access grants, которого в платформенной модели нет. `AuthService` стал
фасадом — публичный API (`authenticated`/`username`/`roles`/`canEdit`/
`hasAnyRole`/`logout`/`accountManagement`) сохранён, поэтому shell, guard и
экраны не менялись; нормализация легаси-имён ролей осталась (локальный realm и
VPS ещё на старых именах). Проверено браузером на локальном Keycloak: открытие
приложения уводит на страницу входа Keycloak, после входа возвращает на
`/home`, все 12 запросов в `/api` идут с Bearer, в localStorage после входа
пусто (на время редиректа keycloak-js держит там временный `kc-callback-*` с
PKCE-верификатором), выход уводит обратно на страницу входа. ⚠️ На стенде у
клиента `iqport-dpport` должны быть прописаны **Valid redirect URIs** и **Web
origins** его домена — иначе Keycloak ответит 400 `Invalid parameter:
redirect_uri` (у нашего VPS это уже починил тимлид, `docs/KEYCLOAK_HANDOVER.md`
§1).

⚠️ **Стенда два, и Keycloak у них разные.** Контейнеры выше настроены на
корпоративный контур (`config.yaml`: база 176.53.160.9, Keycloak
`uport1.ru`). Наш VPS `95850.koara.live` — отдельный стенд со СВОИМ Keycloak,
который отдаётся тем же nginx (same-origin). Адрес Keycloak зашивается в бандл
ПРИ СБОРКЕ фронта, поэтому production-сборка на VPS ломает вход — уводит логин
на чужой сервер (мина описана в `docs/KEYCLOAK_HANDOVER.md` §5.2, решение за
владельцем, файлы `environments/*` самовольно не трогать). Выбор сделан явным:
`docker build --build-arg NG_CONFIG=...`, дефолт `production` = корпоративный
контур.

Открытое: ingress стенда и внешний порт фронта за DevOps; у клиента
`iqport-dpport` корпоративного realm'а нужны **Web Origins** (домен стенда) и
**Direct access grants** — вход идёт формой (`grant_type=password`) прямо в
Keycloak, без них браузер зарубит запрос по CORS; redirect URIs и времена жизни
токенов — TODO в `docs/KEYCLOAK.md` §7. На нашем VPS redirect URI уже починен
тимлидом, там же роли розданы через группы (`docs/KEYCLOAK_HANDOVER.md`). ⚠️ Если CI/CD не подставит `PG_PASSWORD`/Vault-секрет,
бэкенд-контейнер уходит в crash-loop (падать громко — так и задумано): в логе
`password authentication failed for user "gtport_app"`.

Ролевая модель (решение владельца 31.07.2026, справка — `docs/ROLES.md`; общий
realm стенда описан в `docs/KEYCLOAK.md`): realm-роли Keycloak с суффиксом
модуля — `admin_dpport`, `operator_dpport`, `client_dispatcher_dpport`,
`client_dpport` (суффикс разводит наш модуль от соседних в общем realm'е).
⚠️ **Роли НЕЗАВИСИМЫ**: иерархии с весами больше нет, старшая не включает права
младших — доступ есть членство в НАБОРЕ (`auth.Writers` = operator+admin,
`auth.Admins` = admin; наборы в `internal/auth/claims.go` — единственное место,
где записано «кому что можно»). Чтение — любому залогиненному, любая мутация —
`RequireRolesForWrites(Writers)` на всей группе `/api/v1`, `/admin/tables*` —
`RequireAnyRole(Admins)`. Прежние имена (admin/operator/administrator/
dispatcher/client…) нормализует `auth.TokenRoles` при разборе токена — боевые
токены работают до перевода realm'а (шаги миграции — в ROLES.md); значение
`reject_older_role_exempt` из БД матчит админа через want-нормализацию в
`HasRole`. ⚠️ **`keycloak.strict_roles: true` (стендовый `config.yaml`)
выключает легаси-нормализацию токена**: в ОБЩЕМ realm'е голые admin/operator/
dispatcher — легаси-роли контура и могут принадлежать пользователям чужих
приложений (см. KEYCLOAK.md §3.4), превращать их в наши — дыра; принимаются
только точные `*_dpport`. Фронт: `core/auth/roles.ts` (OPER/ADMIN — имена
`*_dpport` + нормализация прежних, UI-уровень), `AuthService.canEdit()`
скрывает кнопки действий на главной для клиентских ролей; рабочие разделы
меню — OPER, «Главная» — всем (просмотр). Фронт правится вместе с бэком:
после перевода Keycloak сервер пустит, а интерфейс спрячет кнопки.

Жизненный цикл статусов (PR #88, отход от gtport по решению владельца): заморозки
на 10 нет — прибывший обновляется и переходит 10→12 (веха выгрузки в историю);
sticky-10 против мигания date_prib; доноры перегруза не из ≥10; пропажа со
статусом 6/10/12 — штатное выбытие (статус 8 только для незавершённого рейса);
выбытие-10 даёт авто-веху выгрузки в историю (момент выбытия, ручную не перетирает);
план неприкосновенен для ≥10. Справка — `docs/STATUSES.md`.

Порожние ПОД ПОГРУЗКУ — не статус 6 (решение владельца 04.08.2026, отход от
gtport): изначально пустой вагон, едущий в адрес порта ЗА грузом, живёт обычным
деревом статусов движения (2/4/5/9/10 — виден подвод, брошенные, прибытие), а не
слипается в неподвижный 6. Критерий `DirectoryCache.PorozhInbound`: признак
порожнего И (вес 0/пуст ИЛИ код груза порожнякового рейса — справочник
`porozh_cargo`, миграция 000057, ЕТСНГ 421xxx + 693157, правится в Админе).
Опустевшие по дороге и выгрузившиеся в порту несут вес и код ПРЕЖНЕГО груза
(сверено с боевой копией 04.08: все 388 вагонов с признаком — уголь ~70 т) —
им 6/12 как раньше, донорская логика цела; заодно закрыта щель ложного донора
при мигании признака (вагон с весом 0 в 6 не попадает). Сопровождение: выбытие-10
порожняка НЕ пишет авто-веху выгрузки (погрузился и уехал — не выгрузка; рейс в
истории с прибытием без выгрузки), он не в счётчике «не выгружено» Оперативки и
не сигналит в «Без плана в подходе». ⚠️ В боевой ленте изначально порожних пока
не встречалось (в снимке ни одного веса 0 и ни одного кода 421xxx) — критерий
проверен тестами, первый живой заход порожняка посмотреть в бою. Отдельная
карточка «Подвод порожних» на главной — следующая итерация (фронт признак может
вычислить: porozh_priznak=1 и статус не 6/12).

Память о задержках вагонов (решение владельца 07.2026): таблица `vagon_delay` —
эпизоды статусов 4/5 на вагон («стоял на станции с… по…», reconcile `applyDelays`
рядом с `applyBros`); эскалация 4→5 затирает kind в том же эпизоде И проходит
сквозь sticky-4 в carryover (иначе бросок на станции долгого простоя не попадал
в `bros` — починенный баг; там же закрыта дыра повторного бросания — поднятый
бросок с тем же ключом переоткрывается UPDATE'ом, а не падает дублем PK).
Автоочистка `delay_cleanup_days` (0 = бессрочно). В «Истории движения вагона» —
сводка «в рейсе N, из них стоял X» + подсветка визитов-задержек. Экраны (решение
владельца 27.07): «Задержанные вагоны» из «Информации» — кто стоит сейчас,
агрегация по индексу поезда с разворотом до вагонов, терминал (колонка
`gruzpol_s`, миграция 000048), фильтр по брошенным; внутри кнопка «Отчёт по
простоям» за период с выбором терминала (аналогия с отчётом «Брошенных»).
`GET /dislocation/delays/current|report`. Нерабочий парк (решение владельца
27.07): вагоны с кодами операций `exclude_oper_codes` (дефолт 72 «Списание»)
вычёркиваются из снимка на Stage 1 — их нет нигде; выбытие штатное, без
записи-8 (случай 54117452: списанный 4 года висел в статусе 4).

Домашняя страница (рабочая зона): три равные колонки — «Оперативка» (сверху
карточка «Статус системы»: актуальность снимка/планов вертикально, как gtport
StatusPanel, + часы МСК/ЖД, + действия диспетчера «Обновить из АСУ» и «Приём ЛК»
перемещаемой модалкой — функционал страницы «Дислокация» перенесён сюда, сама
страница убрана из меню, маршрут жив; рядом карточка «Информация»: счётчики
пропавших и доноров перегруза (ст. 6), клик — модалка с таблицей (у пропавших —
агрегированная, см. ниже), ПКМ по строке даёт историю движения вагона; под
«Информацией» — узкая карточка «Прибытие/
выгрузка»: суточные счётчики по терминалам, прибыло/выгружено одной ячейкой
«П/В» за вчера+сегодня ЖД-сутки из вех vagon_history + «не выгружено» статус-10
из снимка; `GET /dislocation/operativka`, один ответ на две карточки)
+ отдельная жёлтая карточка «Без плана в подходе» (таблица unplanned_move: на сравнении
снимков ловится смена станции без плана ближе порога unplanned_move_km=1000 для
терминалов плановых станций; «Скрыть»/автоснятие планом-прибытием)
· Мыс · Находка (раскладка из `GET /dislocation/terminals`, по коду станции); блок
«Прибывшие» — компактная карточка (топ-5: Прибытие·Индекс·Состав) + перемещаемая
модалка «История прибывших» (`GET /dislocation/arrivals` из vagon_history, группы
index_pp+date_prib) с редактированием по выбранным вагонам (`PUT /arrivals/vagons`:
изменить/отменить прибытие, выгрузка, назначение; только история, снимок не
трогается; не-админам — лишь сегодня/вчера) и экспортом в Excel; кандидаты
прибытия (статус 9, отличие от gtport): жёлтая секция в истории + бейдж в
карточке, «Подтвердить» пишет в снимок (10+date_prib, живёт sticky-10, веха в
историю), «Скрыть» — пометка dismissed_at в status9 до новых данных; ПКМ по
вагону в истории прибывших — «История движения вагона» (перемещаемая модалка):
сначала сохранённый трейл рейса из БД с ПЕРИОДОМ полученной истории (первая и
последняя операция), кнопка «Обновить из АСУ» = запрос 601 `date_nach−1…сегодня`
(пусто в базе — запрос уходит сразу; отказ провайдера тостом, экран не стирается),
строка = визит станции (непрерывная серия операций: первая/последняя, разворот —
все), коды сматчены (`stations`/`cargo_operations`), индекс нормализован 4-3-4,
экспорт в Excel полным трейлом (ширины в единицах Excel + центрирование,
`xlsx-js-style`); блок
«Ближайшие поезда» (`GET /dislocation/nearest`, время план→прогноз→расчёт,
плановые зелёным, брошенные красным; ПКМ: натурный лист/СМС/Excel с
Собственником=owner, печать таблицы); блок «Информация» — следующая итерация.

Грузовая работа (перенос страницы gtport, PR #132): суточный учётный лист
терминала — кнопка в карточке «Информация» открывает перемещаемую модалку
(таблица «Показатель × линия груза» + блок погрузки, цвета терминалов из
`ports.color`). Порт больше не в имени таблицы, а род груза не в имени колонки:
состав колонок задаёт справочник `port_cargo_line` (kind unload/load, своя
способность ваг/сут, метка колонки плана для «Остатка на 18:00»), правится в
Админе. Авто-слой (прибытие, ВЫГРУЗКА ПО СТАНЦИИ из вех истории — отход от
gtport, где её вбивали руками, остаток прошлых суток, остаток из плана,
движок суточной аналитики) пересобирается кнопкой «Пересчитать» и не затирает
ручные план/факт/комментарий; производные (остаток, эффективность, перепоказ)
считает сервер. Права: admin — любые сутки, остальные — только вчера.

Пропавшие вагоны — агрегированный вид + «Подтвердить прибытие» (решения
владельца 02.08.2026): модалка из «Информации» показывает записи-8 группами
«поезд → подгруппа → вагоны» (ключи как у кандидатов прибытия, display-строки
как у прибывших), с выбором вагонов кликом/ПКМ. Смысл действия: из-за
дискретности выгрузок дислокации вагон мог прибыть, выгрузиться и выпасть из
потока без статусов 9/10/12 — диспетчер восстанавливает факты руками: прибытие
обязательно, выгрузка опционально ТОЙ ЖЕ формой (чекбокс «Выгружен», место —
пусто = терминал назначения). Реализация: `POST /dislocation/missing/confirm`
(`ArrivalsService.ConfirmMissing`) пишет веху в vagon_history (статус 10/12,
поля — эталоны ConfirmArrival/«Выгрузить») и снимает запись-8; СНИМОК НЕ
ТРОГАЕТСЯ — вагона в нём нет, вернётся в дислокацию — поток вернее ручных вех.
Права: operator+, «сегодня/вчера» НЕ применяется (смысл функции — старые даты);
валидация по данным: time_op ≤ прибытие ≤ выгрузка ≤ сейчас (в МСК-эквивалентах).
Журнал `confirm_missing`. Страница `/missing` осталась старым плоским видом;
модалка доноров (ст. 6) не тронута. В диалоге также правка индекса поезда
(index → index_pp, сильнее индекса записи-8).

Единая шкала ручного ввода времени (решение владельца 03.08.2026). Историческая
асимметрия gtport: date_prib в vagon_history — ЖД-штамп (оператор вводил ЖД),
date_vigr — МСК-факт (вводил МСК); сдвиг «час ≥ 18 → ±сутки» для прибытия делал
оператор, для выгрузки — сервер. Теперь диспетчер вводит ВСЕ времена диалога в
одной шкале — переключатель ЖД/МСК во всех четырёх диалогах правок (подтвердить
пропавшего/кандидата, изменить прибытие, выгрузить); недостающие сдвиги считает
СЕРВЕР по `time_base` запроса (хелперы arrivalToJd/unloadToMsk; пустой time_base
= прежнее смешанное поведение, совместимость). Хранение НЕ менялось. Дефолт
шкалы — `client_settings.extra.ui.time_base` (наш клиент — «jd»), фронт читает
`GET /settings/ui` (TimeBaseService); переключение диспетчером действует на
сессию во всех диалогах, дефолты полей показываются и пересчитываются в
выбранной шкале. План (plan_jd) шкале не подчиняется — всегда ЖД. В журнале
правок — `time_base`. Проверено обеими шкалами на одном событии: хранимые вехи
идентичны.

Фронтенд (Angular 21 + ng-zorro): экраны «Дислокация» (ЛК + АСУ + статус-панель с
часами МСК/ЖД), «Пропавшие вагоны» (записи-8 из status9, последняя позиция;
в меню нет — открывается модалкой из «Информации», как и «Грузовая работа»),
«Перестановки/Переадресация» (стиль gtport: сводные колонки + диалог управления +
панель станций с drag&drop в справочник + карточки направлений; ПКМ с автовыделением;
правила целей: перестановка — терминалы своей станции, переадресация — чужих + ВП,
«сам на себя» не предлагается; pereadr_* вместо info_1/2; батч = один пересбор),
«План подвода» (вид gtport: ЖД-даты с разделителями, фильтр по дате, печать таблицы),
«Прогнозы», «Админ». Уведомления — только тостами (NzMessageService).

Интеграции с внешним провайдером (тот же адрес, `X-API-Key`, самоподписанный TLS):
- **Дислокация АСУ-АСУ** (в `main`): исходящий клиент с `auth_header`/`insecure_tls`;
  крон выровнен по стеночным часам (`worker.NewAlignedCronWorker`, config `asu.*`:
  `pull_interval` дефолт 10m + `pull_offset`, у нас 5m → :05,:15,...); ручка
  `POST /dislocation/asu/pull`. В бою, крон включён.
- **История продвижения вагона (запрос 601)**: `GET /wagons/{vagon}/history/{client}
  ?from&to` (контракт проверен боем 2026-07-21); авто-заявки конвейера (прибытие →10,
  пропажа незавершённого, выбытие прибывшего; интервал date_nach−1…сегодня; каждая
  история затирает прежнюю — `ReplaceForTrip` в `vagon_operation`) через очередь в БД
  `vagon_op_request` (upsert по trip_key — дедуп групповых смен), фоновый воркер
  пачками 50 с паузой 500мс (config `wagonops.*`); клиент провайдера — из
  `ports.provider_client` по ОКПО грузополучателя; ручной запрос — синхронная ручка
  `POST /dislocation/vagons/{vagon}/operations/pull` (UI-модалка — следующая итерация).
- **Памятки ГУ-45 на подачу/уборку** (решение владельца 28.07.2026): крон-инкремент
  `<client>/reference/update` заполняет вехи рейса в `vagon_history` — в gtport их
  вбивали руками Excel-файлом «Журнал движения» (`JournalUploadM`), теперь ведёт
  крон. Клиенты провайдера (`attis`, `nmtp`) — в пути запроса; курсор `LAST_UPDATE`
  живёт в `pamyatka_cursor` (двигается только после успешной записи и только
  непустой). Раскладка (в модели gtport стояло «уточнить»): подача → `nom_gu45_pod`/
  `date_gu45_pod`(дата документа)/`date_pod`(GET_IN)/`place_pod`; уборка →
  `nom_gu45_ubor`/`date_gu45_ubor`/`date_ubor`(GET_OUT); `REPORT` → `date_vigr_gu45`
  (веху `date_vigr` из истории АСУ не перетирает); дубли `date_pod_gu45`/
  `date_ubor_gu45` остаются пустыми. Стадия — `pamyatka_state` 0/1/2 (миграция
  000049). Матч рейса — **замок по времени прибытия** (последний рейс с `date_prib`
  ≤ подачи): по одному номеру вагона треть строк садилась на посторонний рейс,
  памятки приходят задним числом. Уборочная памятка на стадии 0 заполняет оба
  блока (GET_IN в ней есть всегда, уборок приходит больше, чем подач); повтор того
  же документа обновляет на месте; чужая памятка перезаписывает рейс, только если
  полнее. Движок — `domain.ApplyPamyatki` (чистая функция, 11 тестов); итог прохода
  — в `event_journal` (`pamyatka_pull`). Сами памятки НЕ храним: нужны вехи рейса.
  Забор по номеру — диагностический, отдаёт сырой документ (иной контракт: обёртка
  ГУ-45 с `_decoded_doc`). Прогон на боевых данных 28.07: 167 памяток → 1275 рейсов,
  2019 строк мимо (чужие вагоны и рейсы старше нашей истории). Крон включён
  (`enabled: true`, 1h).
  ⚠️ **Холодный старт заклинивал — починено 30.07.2026** (проверка боем по просьбе
  владельца: в `vagon_history` не было НИ ОДНОЙ вехи из памяток — 0 из 12862 рейсов,
  `pamyatka_state`=0 везде, `pamyatka_cursor` пуст, 80 тиков за 3 суток все по нулям).
  Причина — окном первого захода служил сам `pull_interval` (час), а **провайдер
  публикует памятки позже их собственного `LAST_UPDATE`** (пробником в 02:52 МСК:
  окна 1h и 3h — пустое тело 32 байта, 12h — attis 8 памяток/233 вагона, nmtp 9/195,
  36h — 22/533 и 52/1160; верхний штамп провайдера отставал на 4–6 ч). Пустой ответ
  не даёт `LAST_UPDATE` → курсор не сохраняется → следующий тик снова первый заход.
  Разведено на три параметра (`config reference.*`, дефолты в коде):
  `initial_lookback` 48h — окно, когда курсора нет; `cursor_overlap` 30m — курсор
  храним раньше `LAST_UPDATE`, иначе запоздавшие записи (провайдер публикует не по
  порядку штампов) терялись бы навсегда; `stale_after` 6h — пустой проход при
  неподвижном курсоре пишется в журнал (`silent_hours`), чтобы простой было ВИДНО:
  прежде `appendJournal` молча выходил на `pamyatki == 0`, и трое суток простоя
  выглядели в интерфейсе как тишина. Курсор двигается только вперёд, повтор пачки
  нахлёста в журнал не пишется (`advanced`). Крон-тик добавлен и при старте
  (`worker.CronWorker.WithRunAtStart`) — рестарты деплоя отодвигали часовой тик.
  Тесты: холодный старт, нахлёст, откат назад, порог молчания.
  ⚠️ **Контракт забора по номеру сменился 30.07.2026** (провайдер, обратной
  совместимости нет). Памятку определяет ТРОЙКА «клиент + номер + дата создания»:
  номер совпадает у разных портов и переиспользуется внутри одного. Стало
  `GET /{client}/reference?number=10326&date_create=07-04-2026` — параметр
  обязателен (нет → 400), значение отдаём ДОСЛОВНО как пришло в `DATE_CREATE`
  инкремента (принимается MM-DD-YYYY и YYYY-MM-DD; DD-MM-YYYY отвергается
  намеренно — для чисел ≤ 12 неотличим от MM-DD-YYYY и вернул бы чужой документ).
  Пустое значение и отсутствие параметра — РАЗНЫЕ вещи: у документа без даты
  оформления `DATE_CREATE` приходит пустой строкой, и её надо передать пустой.
  Тело ответа не изменилось (сверено боем 30.07: конверт и пачка те же, 5
  документов, номера внутри различны). Наши ручки: `GET /reference` и
  `/reference/excel` требуют `date_create` (400 с пояснением, если параметра нет);
  «отдать только запрошенный документ» просить у провайдера не нужно — мы и так
  выбираем его сами (`ParseReferenceDocByNumber`).
  **Второе изменение — смысл `DATE_CREATE` в инкременте:** было MIN(GET_IN_DATE)
  с подставленным текущим годом, стало дата оформления ГУ-45 с настоящим годом
  (у 10326 NMTP разошлось на 6 суток: 06-28-2026 → 07-04-2026). Для нас это и
  веха `date_gu45_pod/ubor`, и опора для подстановки года в даты вагонов («дд.мм»
  без года) — опора не хуже прежней. Пустой `DATE_CREATE` больше не роняет разбор
  памятки: год берём от `clock.Now()`, а колонку даты документа не пишем вовсе
  (не затираем NULL'ом прежнюю). Перезабирать ранее сохранённые `DATE_CREATE` не
  пришлось: в `vagon_history` их не было ни одного (см. заклинивший холодный старт
  выше).
- **Сырая памятка (забор по номеру) — разобрана** (`parser.ParseReferenceDocByNumber`
  → `domain.PamyatkaDoc`; берём ТОЛЬКО искомый документ, решение владельца 28.07 —
  попутчики не разбираются, их поломка не отнимает исправную памятку. Разбор пачки
  целиком — `ParseReferenceDoc`, для диагностики. Golden-тесты на боевых ответах
  обоих клиентов). Контракт
  выяснен боем 28.07: конверт `{code,status,message,received_at,data:{count,
  documents,new_oper_id}}`, документ = `_summary` (реквизиты обработки) +
  `_decoded_doc` (бланк; он же полный декод base64-XML из `getPPSReply`) +
  сам `getPPSReply`. **Номер отдаёт пачку целиком, а не один документ** — у обоих
  клиентов, не только у nmtp: провайдер по номеру находит пачку-контейнер
  (attis `number=11013` и `number=11017` дают байт в байт один ответ из 5
  документов); нужный документ выбирается на нашей стороне (`ByNumber`).
  Несуществующий номер — HTTP 404 `{"error":"pamyatka not found"}`. **Документы
  внутри пачки между собой не связаны по смыслу**: общие у них только клиент
  (orgid/ОКПО владельца пути — 20/20 пачек) и узкое окно времени операции
  (медиана 1.3 ч; порции идут встык, нахлёста по времени почти нет), порядок —
  по `DocId`, т.е. по созданию в источнике (20/20). Станция одна лишь в 10/20
  пачек, путь подачи всегда разный, подача и уборка перемешаны, а **вагоны
  пересекаются у 1 пары документов из 986**. Пачка — подшивка «оформлено в тот
  же час», а не дело; «попутчиков» нельзя считать связанными с запрошенным.
  Один документ попадает в две пачки, если его повторно тронули (`DocLastOper`
  разный) либо порцию пересобрали (тот же `DocLastOper`) — 12 из 168. Ловушки:
  `wagons.wagon` у памятки на один вагон приходит **объектом**, а не массивом
  (5 из 18 документов nmtp); `received_at` — единственное поле с `Z`, держим
  строкой без конверсии. Сверх выжимки инкремента бланк даёт код/имя груза по
  вагону, код станции ЕСР, собственника вагона, реквизиты сторон. Разбор пока
  ни к чему не подключён — ручка по-прежнему отдаёт байты как есть.
  Прогон 28.07 на 20 запросах (10 подача + 10 уборка, оба клиента, документы
  07.07–27.07) → 168 документов, 2364 вагона: разобрались все, времена рейса
  везде по возрастанию (подача ≤ уведомление ≤ уборка), `containers` непустым
  не встретился ни разу. Пачка на запрос — 1…19 документов (бывает и `count=1`).
  Из `getPPSReply` берём один блок — `Documentdata.Metadatatab` (решение владельца
  28.07): `doc_creator` (кто составил), `signatories` (кто подписал), `doc_date`+
  `doc_time` (когда СОСТАВЛЕН — раньше `DocDate`, момента регистрации: у 10807
  21:45 против 21:51:02; проверено, `ComposedAt ≤ DocDate` на всех 169 документах),
  `railway_code`. ЭЦП (`Docsigntab`, килобайты PKCS#7) не разбираем. Реквизиты
  сторон плавают по документам, а не по клиентам: у attis тоже встречается блок
  контрагента (10807 — АО НМТП, name+okpo без ИНН/КПП).
- **Печатный бланк ГУ-45 в Excel** (`GET /reference/excel?number=&client=`):
  разобранная памятка раскладывается по образцу печатной формы
  (`internal/report/pamyatka_gu45.go`) — шапка, 12 граф в порядке бланка, подвал
  с местом для отметок; A4 книжная, вписано по ширине, шапка таблицы повторяется
  на каждой странице. Чего нет в источнике, того нет и в бланке: графы 9–11
  (задержка, № акта ГУ-23, взвешивания) и индекс поезда — пустые линии под
  ручное заполнение. Подпись приемосдатчика ставится со стороны «Сдал» на
  подаче и «Принял» на уборке (как в обоих боевых бланках); сертификаты ЭЦП не
  печатаем (`Docsigntab` не разбираем) — вместо них блок «составил / подписи /
  составлен / зарегистрирован». Кнопки в UI пока нет, ручка отдаёт файл.
- ⚠️ **Инкремент склеивает одноимённые памятки — на нём сейчас работает конвейер.**
  Номера памяток у клиента циклятся (11224 был и 01.07, и 24.07), и в выжимке
  `reference/update` строки разных документов с одним номером приходят ОДНОЙ
  записью: у attis/11224 от 24.07 — 30 вагонов, из них 20 с подачей 24.07 и 10
  с подачей 30.06–01.07. Сырой бланк того же документа чист (20 строк, сверено
  с исходным XML внутри `getPPSReply` — декод провайдера ничего не теряет).
  Масштаб на выборке 28.07: **23% памяток (96 из 424) и 17.7% вагоно-строк
  (1507 из 8499)** несут строки старше документа более чем на неделю. Значит
  `ApplyPamyatki` получает чужие строки; замок по времени прибытия часть из них
  гасит, но не все. Разбор сырого бланка даёт способ чинить — брать состав
  вагонов из документа, а не из выжимки. Решение владельца не принято, конвейер
  не трогали.

«Справки и отчёты» (решение владельца 29.07.2026, начало переноса отчётов):
в gtport один набор отчётов был скопирован в ТРИ вкладки («Справки»,
«Справки и отчёты», «Инструменты оператора → Типовые отчеты» — «Справки»
вообще байт-в-байт поднабор). В DPmodule он живёт один раз — страница
«Справки и отчёты» (`features/reports`, ex-`reference`); пункт «Справки»
убран из меню, `/reference` редиректит. Порядок переноса отчётов: Подход
(+Марис) → Погрузка/Выгрузка (4 отчёта) → НМТП (последним, с пересмотром
сборки и таблицей `report_column_mappings` из TARGET.md §3.6); за
«Инструментами оператора» остаются только дислокация/история (отдельная
волна). Перенесено в этом заходе: **Подход** (RAM-агрегация `aggregatePodhod`
+ golden-тест; клиентские варианты — таблица `report_preset`, миграция
000050, правится в Админе; «Марис» — сид, не хардкод; Excel на клиенте по
раскладке серверного файла gtport; «В MAX» картинкой по маршрутам формы
`podhod` в `max_route`, пресеты — сводным маршрутом) и **Повагонка**
(`report.VagonkaXLSX`, серверный .xlsx, фильтр `?terminal=`). Отходы от
gtport: исключается и статус 12; формулировки перестановок в примечании
универсальные по станциям из `ports`; «Собственник» повагонки = `owner`
(в gtport ошибочно `rod_vag_uch`); автообновления 5 мин в модалке нет.

Погрузка/Выгрузка (решения владельца 29.07.2026, вторая волна отчётов):
четыре формы gtport схлопнуты в две. **Погрузка** — одна модалка с видами
«Сводка» (терминалы рядом, кол-во+ср.сут с начала месяца, PNG/«В MAX» по
маршрутам формы `pogruzka`, миграция 000051) и «По дням» (пивот дата ×
отправитель, СР.СУТ./ИТОГО, Excel); одна ручка `GET /reports/loading`
(`HistoryRepository.LoadingDaily`, integration-тест). Отходы: перегрузы
исключены (TARGET §3.17); разбивка уголь/металл — по `cargo_group`, а не
по клиенту «ЕВРАЗ ТК» из SQL gtport (сверено на июле: ЕВРАЗ ТК = МЕТАЛЛ
1:1); gtport-баг сравнения с «ЕВРАЗ» не переносится. **Выгрузка** — только
«за период» (`GET /reports/vygruzka` = CargoWorkService.Period над
cargo_work; колонки по линиям `port_cargo_line`, СРЕДНЕСУТ. по дням с
данными, Excel); «Выгрузка за день» — скрин-форма для отправки в MAX
(перенос gtport CargoReport, замечание владельца 29.07.2026: не редактор;
маршруты формы `vygruzka` — миграция 000052; лист правится в «Грузовой
работе»). Повагонка (то же замечание): терминальная выгрузка — короткий
набор gtport «Повагонка Аттис», обобщённый на любой терминал; из полной
убраны «Для СМС1/СМС2» и «Данные броса», добавлены «Заявка», «Марка
груза» (`freight_exact_name`, уточнение владельца) и «ГТД» (`gtd_number`);
«Операция с вагоном» в полной — полное имя (`oper`).

Перестановки + раскладка страницы (замечание владельца 29.07.2026): страница
«Справки и отчёты» — два подписанных ряда как в gtport (верхний — текущие
справки в MAX, нижний — отчёты за период и по портам), hover карточек
(подъём + тень; верхняя цветная полоса — перенос вида оригинала, находка
дизайн-хука признана ложноположительной). Блок «Перестановки»:
«Перестановка на {терминал}» — серверный .xlsx текущих перестановок
(листы «Поезда»+«Вагоны», направления из реестра, не хардкод gtport) и
«Факт. перестановки» — модалка за период из vagon_history (репо
`PerestanovkaRows`, «Соответствие» = выгружен на терминале назначения;
ещё не выгруженные расхождением не считаются). «Отчёт по брошенным» и
«Погрузка по дням» (вид той же модалки, вход `initialView`) открываются
с нижнего ряда напрямую.

Отчёты НМТП перенесены (решения владельца 29.07.2026, справка —
docs/NMTP_REPORT.md): «Подход вагонов» по форме порта, эталон — файлы порта
ГУТ-2/УТ-1 (АЭ — по gtport-раскладке, файла нет). Колонки — справочник
nmtp_column (клиент × станции × марка, матч по специфичности, несматченное —
в «прочее» громко), марка угля — нормализатор по freight_exact_name (словарь
nmtp_mark; код ЕТСНГ марку не различает), sprav_1 gtport не реанимировался.
Отбор строк — как у «Подхода» (получатель ИЛИ назначение; в файле порта
поезда «НА АТТИС» стоят с пометкой — gtport-фильтр строго по naznach был
отходом от их формы). Время в книге — владивостокское (+7 только в рендере
report.vladTime — единственный сдвиг в проекте, решение владельца). Тоннаж
фактический по ves (не 0.07×вагоны). Ручных правок gtport (сдвиг прогноза,
приоритет, свои строки) нет — «убираем, но помним». Сверка с листами порта
29.07 на копии боевой: общие поезда в одинаковых колонках, «ЛЕН-КУЗН 1/2» у
порта оказались колонками ПО МАРКЕ (Г / Д,ДОМСШ) — правила так и заведены;
обе формы раскладываются без «прочего». Карточка «Отчёты НМТП» — верхний ряд
«Справок», открывает перемещаемую модалку (экранная форма книги: переключатель
«скрыть перестановки» = gtport naznach-only, «В MAX» картинкой по маршрутам
формы `nmtp` — миграция 000054, PNG, серверный .xlsx). Правила владельца
30.07.2026 (после сверки с gtport-выгрузкой АЭ): состав <20 ваг. — не поезд в
счётчиках; «ожид. прибытие» — только плановым (есть plan_jd); оформление книги
— по стилям файла порта; ключ строки индекс+станция+прогноз (Б/И не слипаются).
Правка справочника АЭ (марки Лен-Куз I=Г, ЛК-1 добавлен в «Терент…») — в бою
через Админ. Редактор модалки (решение владельца 30.07.2026): правки отчёта
НЕ хранятся — только экран и экспорт (Excel с правками = POST правленого
отчёта); хранится лишь привязка «вагон → колонка» (nmtp_vagon_column,
миграция 000055, ПКМ «Переместить в колонку»), она сильнее правил и гаснет
с выпадением вагона из подхода. Экран модалки — вид gtport (зелёные дороги,
голубые брошенные, итоги в ходу/брошенных/на сети, «Итог по грузовым
колонкам»). Перенос отчётов gtport со страницы Tools ЗАВЕРШЁН.

Инструменты оператора (волна «дислокация/история», начата 31.07.2026): экран
«Поезда в движении» перенесён (gtport OperatorToolsDislocation) — пункт меню
«Инструменты оператора» ожил, маршрут `/operator-tools`. Одна ручка
`GET /dislocation/trains`: весь снимок деревом «поезд → подгруппа → вагоны»
(ключ поезда — index_pp|index + станция операции, отцепка отдельной строкой),
фильтры и поиски на клиенте как в gtport (управление/прибытие/терминалы/статус;
3 цифры номера поезда, станция погрузки, груз, списки вагонов и накладных —
последние два режимом плоских карточек), Excel «вся таблица» и «Натурный лист»
поезда, ПКМ по вагону — «История движения вагона». Решения владельца
31.07.2026: статус поезда — честный агрегат-большинство по вагонам (в gtport —
от случайного «первого вагона» недетерминированного обхода), простой — максимум
по вагонам; «прибывшие» = статусы 9/10/12; марка груза = freight_exact_name,
собственник = owner (в gtport колонку заполнял rod_vag_uch); терминалы фильтров
из реестра ports с цветами (не хардкод); фиктивный filter_type=УТ-1
(gtport-сервис его игнорировал и отдавал всё) не перенесён — «все чипы
выбраны» = фильтр выключен, видны и нетерминальные назначения (ВП); «Детали
поезда» (gtport DetailPanel) — перемещаемая модалка по иконке ⓘ и из ПКМ.
Golden-тест — `trains_test.go`.

«Работа с историческими данными» перенесена (решения владельца 04.08.2026,
волна «Инструменты оператора» ЗАВЕРШЕНА): вкладка на `/operator-tools`
(контейнер `features/operator-tools/operator-tools.component.ts`, содержимое по
`@if` — невыбранная вкладка не инстанцируется), перенос gtport
OperatorToolsHistory. Read-only таблица 26 колонок по `vagon_history`, данные
только по «Применить», верхний горизонтальный скроллбар синхронизирован с
таблицей (у таблицы скролл по X скрыт — как в оригинале). Главный отход:
**серверные пагинация и сортировка** (`POST /history/search`: фильтр + белый
список 26 колонок сортировки + limit 100/500/1000; в gtport — захардкоженный
limit 5000 без листания и клиентская сортировка, а в бою ~100 тыс. записей —
грузить всё нельзя). Прочие отходы: терминалы чипов из реестра `ports` с
цветами (не хардкод); «не выгруж.» опирается на схему `place_vigr NOT NULL
DEFAULT ''` (NULL-дефект gtport невозможен by design); фильтр выгрузки по
`date_vigr_d` (ЖД-сутки), а не timestamp `date_vigr` (gtport терял последний
день диапазона); Собст = `owner`, Марка = `freight_exact_name`, ГТД =
`gtd_number`, «Перегруз» = `peregruz` (в gtport «Отгружен» ← info_3 — подпись
вводила в заблуждение); статусы — полный набор DPmodule; ПКМ по строке —
«История движения вагона» (в gtport экрана ПКМ не было); Excel строит СЕРВЕР
по всему фильтру (`POST /history/excel`, `report.HistoryXLSX` StreamWriter'ом,
потолок 100 тыс. строк с ошибкой «сузьте фильтры»; замер на копии боевой:
100 тыс. строк ≈ 5 с и 11 МБ — запас до `write_timeout: 30s` многократный);
словарь станций погрузки — DISTINCT по самой истории (`GET /history/meta`),
не marka. Поиск и Excel — POST (списки вагонов из Excel в query string не
влезут); побочно POST требует operator+ (`RequireRolesForWrites`) — приемлемо,
страница и так OPER; если историю когда-то открывают клиентским ролям,
понадобится исключение пути в мидлвари. Строки со старыми именами терминалов
(переименованных в `ports`) чипами не достать — только списком вагонов.
Тесты: `history_search_internal_test.go` (валидация),
`history_search_integration_test.go` (SQL-фильтры/пагинация/сортировка, гейт
`DPMODULE_TEST_PG_DSN`), `report/history_test.go`. Проверено вживую браузером
на копии боевой (11 612 рейсов): вкладки, фильтры, сортировка/страницы по
всему набору, «не выгруж.» 6846, синхронный верхний скролл, Excel 985 КБ/0,5 с,
ПКМ-трейл; иконки clear/up/warning/check-circle/caret-* дорегистрированы в
`app.config.ts` (грузились динамически с 404 в dev).
Вкладка «Прогноз прибытия/выгрузки» — перенос страницы «Прогноз GT» gtport
ЗАВЕРШЁН (решения владельца 05.08.2026, три этапа/коммита: просмотр → what-if →
снапшоты). Страница «Прогнозы» стала контейнером вкладок (nz-tabs, «Новый
прогноз» не тронут). Ключевые решения: симуляция выгрузки считается НА СЕРВЕРЕ
(пакет `internal/service/unloadsim`, порт браузерного simulation.ts gtlogic;
golden-фикстуры сгенерированы прогоном САМОГО эталона —
`scripts/golden/gen_gtsim_fixtures.mts`, нужен клон gtlogic рядом, есть только
в WSL); прогноз прибытия и what-if — ЕДИНЫМ алгоритмом DPmodule stage4
(pc-интервалы, реш. 000026), НЕ фиксированными интервалами gtport — вкладка
показывает те же prog_jd, что и остальные экраны; предпросмотр правок — быстрый
POST simulate (~15-25 мс расчёт; отклик на боевом VPS проверить после деплоя,
при жалобах — доложить локальную оценку). What-if: бросить/восстановить/на
нитку/переместить универсальный груз (naznach_station.univers), stateless —
весь список правок сеанса в каждом запросе, журнал на фронте; stage4 расширен
`Train.Delay` (замещает штраф Bros; конвейер не заполняет — бит-в-бит) и
`Config.StartTime` (фиксированный старт — правка не двигает чужие прогнозы).
Скорости: план — `port_cargo_line.plan_speed` (000058, сид дефолтов gtport,
правится в Админе), норма — существующий `pc`; таблицу gt_port_speeds не
заводили. Снапшоты — `gt_forecast_snapshot` (000059, ключ дата×станция; сервер
пересчитывает по входу сеанса, НЕ доверяет расчёту фронта), архив read-only на
самой вкладке, CSV-аналитика «прогноз vs факт» ZIP'ом (факт из vagon_history,
отклонение в расчётной шкале), Excel — xlsx-js-style без html2canvas (сводка
числами, вопрос картинок диаграмм отложен). Осознанные отходы от эталона:
задвоение поезда с нулевой частичной выгрузкой исправлено (тест-регрессия),
удаление снапшота — operator+ (в gtport admin-only), «Прогноз Макс» — не эта
вкладка и не переносился. Замечания владельца после приёмки (05.08.2026):
свободные нитки — НЕИСПОЛЬЗУЕМЫЕ нитки текущего плана подвода (слоты
nitka_schedule минус нитки свежего плана по его датам; как unused_slots
gtport), а не «слоты каждый день горизонта»; скорости правятся и из вкладки
(шестерёнка → PUT /gt-forecast/speeds → port_cargo_line, operator+);
выделение поезда кликом синхронно в таблице и на Ганте, ПКМ работает и на
Ганте; План/Норма — переключателем.

Сверка с ветками тимлида `backend-development`/`frontend-development` (срез
05.08.2026, архивы zip). Принято к нам: переименование конфига стенда в
`config.yaml` (в git, см. блок про Vault выше; боевой конфиг → `config.vps.yaml`),
адрес памяток `reference.base_url` в стендовом конфиге, расширенный
`.dockerignore`, стыковка фронта с порталом-лаунчером (`portalUrl` в
environments, каталог `modules.config.ts` с ролями `view_*`/`edit_*` и
признаком `available`, пункт «Портал — все модули» в свитчере,
`frontend/docs/PORTAL_INTEGRATION.md`). НЕ принято: (1) в их ветке СНЯТ
strict-режим ролей (`keycloak.strict_roles`, `auth.TokenRoles`) — дыра
легаси-ролей общего realm'а, которую мы закрыли в `dc0a811`, у них снова
открыта; наш код не трогали, в стендовом конфиге `strict_roles: true` оставлен,
тест `examples_test.go` теперь сторожит строку; ⚠️ сказать тимлиду. (2) У MPPort
в их каталоге остался `http://localhost:4202` — заменён боевым адресом.
(3) Фронт-сервис из бэкендового compose у них удалён (на стенде пары нет) — у
нас оставлен для локальной проверки пары. Их ветки отстают от нашего `main`
примерно на два дня (нет gt-forecast, поиска по истории, правок nginx 04.08).

<обновляй по ходу>
